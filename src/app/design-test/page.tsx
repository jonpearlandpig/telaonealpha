import TELACardDemo from '../../../design-system/TELACard';

export default function DesignTestPage() {
  return (
    <main style={{ background: 'var(--color-void)', minHeight: '100vh' }}>
      <TELACardDemo />
    </main>
  );
}
